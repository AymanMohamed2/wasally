import 'package:flutter/material.dart';

import '../add_shop_view.dart';

class AddShopBottomSheet extends StatelessWidget {
  const AddShopBottomSheet({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(child: SignUpView());
  }
}
